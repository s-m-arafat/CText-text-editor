
// all the terminal related functions to modify the terminal are declared here. the corresponding definitions are in ./terminal.cpp 
#ifndef TERMINAL_H
#define TERMINAL_H

// enter raw mode to read input byte by byte instead of line by line
void enableRawMode();

#endif
